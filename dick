-- // TINTWARE | RE-INJECTION SUPPORTED // --

-- // SCRIPT KILLER (Prevents Interference) //
if getgenv().TintwareExecuted then
    getgenv().TintwareExecuted:Disconnect() -- Kills old Heartbeat
    if getgenv().TintwareInput then
        getgenv().TintwareInput:Disconnect() -- Kills old Keybind
    end
end

local Players = game:GetService("Players")
local RS = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local Stats = game:GetService("Stats")
local StarterGui = game:GetService("StarterGui")
local Client = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local MainEvent = game:GetService("ReplicatedStorage"):WaitForChild("MAINEVENT")

-- // STARTUP NOTIFICATION //
task.spawn(function()
    local success = false
    while not success do
        success = pcall(function()
            StarterGui:SetCore("SendNotification", {
                Title = "TINTWARE",
                Text = "made by tint392 on discord",
                Duration = 100
            })
        end)
        task.wait(1)
    end
end)

local LockedPlayer = nil
local Tracer = Drawing.new("Line")
Tracer.Thickness = 1.5
Tracer.Visible = false

local function getPing()
    local pingValue = Stats.Network.ServerStatsItem["Data Ping"]:GetValue()
    return (tonumber(pingValue) or 0)
end

local function notify(text)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = "TINTWARE | TESTING |", 
            Text = text, 
            Duration = 2
        })
    end)
end

-- // AUTO-UNLOCK //
Players.PlayerRemoving:Connect(function(player)
    if LockedPlayer == player then
        LockedPlayer = nil
        getgenv().HyperConfig.Enabled = false
        Tracer.Visible = false
    end
end)

local function isVisible(part)
    if not part or not part.Parent then return false end
    local char = Client.Character
    if not char then return false end
    local params = RaycastParams.new()
    params.FilterType = Enum.RaycastFilterType.Exclude
    params.FilterDescendantsInstances = {char, part.Parent}
    local ray = workspace:Raycast(Camera.CFrame.Position, part.Position - Camera.CFrame.Position, params)
    return ray == nil
end

-- // MAIN HEARTBEAT //
getgenv().TintwareExecuted = RS.Heartbeat:Connect(function()
    local Config = getgenv().HyperConfig
    if not Config or not Config.Enabled or not LockedPlayer then 
        Tracer.Visible = false
        return 
    end

    local char = LockedPlayer.Character
    local targetPart = char and char:FindFirstChild(Config.HitPart)
    local hum = char and char:FindFirstChildOfClass("Humanoid")

    if not targetPart or not hum or (Config.KnockedCheck and hum.Health <= 0) then
        Tracer.Visible = false
        return
    end

    local velocity = targetPart.AssemblyLinearVelocity
    local ping = getPing()
    local pBoost = ping * Config.PingScaling
    
    local finalX = (velocity.X * Config.VelocitySensX) * (Config.PredX + pBoost)
    local finalY = (velocity.Y * Config.VelocitySensY) * (Config.PredY + pBoost)
    local finalZ = (velocity.Z * Config.VelocitySensZ) * (Config.PredZ + pBoost)
    
    local rawPred = Vector3.new(finalX, finalY, finalZ)
    local finalPred = rawPred
    
    if rawPred.Magnitude > Config.MaxPredictionClamp then
        finalPred = rawPred.Unit * Config.MaxPredictionClamp
    end

    local finalPos = targetPart.Position + finalPred + Vector3.new(0, Config.VerticalOffset, 0)
    local tPos, tVis = Camera:WorldToViewportPoint(finalPos)
    
    Tracer.Color = Config.TracerColor
    if tVis then
        Tracer.Visible = true
        Tracer.From = UIS:GetMouseLocation()
        Tracer.To = Vector2.new(tPos.X, tPos.Y)
    else
        Tracer.Visible = false
    end

    if isVisible(targetPart) and Config.TriggerBot then
        local tool = Client.Character:FindFirstChildOfClass("Tool")
        if tool and not tool:FindFirstChild("Safe") then
            MainEvent:FireServer("MOUSE", finalPos)
            tool:Activate()
        end
    end
end)

-- // INPUT BIND //
getgenv().TintwareInput = UIS.InputBegan:Connect(function(i, gpe)
    local Config = getgenv().HyperConfig
    if gpe or not Config then return end
    
    if i.KeyCode == Enum.KeyCode.Q then
        if Config.Enabled then
            Config.Enabled = false
            LockedPlayer = nil
            Tracer.Visible = false
            notify("Unlocked")
        else
            local mousePos = UIS:GetMouseLocation()
            local closestPlayer = nil
            local dist = math.huge
            
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= Client and v.Character and v.Character:FindFirstChild(Config.HitPart) then
                    local tp = v.Character[Config.HitPart]
                    if isVisible(tp) then
                        local p, os = Camera:WorldToViewportPoint(tp.Position)
                        if os then
                            local d = (Vector2.new(p.X, p.Y) - mousePos).Magnitude
                            if d < dist then 
                                closestPlayer = v 
                                dist = d 
                            end
                        end
                    end
                end
            end
            
            if closestPlayer then
                LockedPlayer = closestPlayer
                Config.Enabled = true
                notify("Locked onto " .. closestPlayer.DisplayName)
            else
                notify("no nigga visible")
            end
        end
    end
end)
